<p id="notice"><%= notice %></p>


<div class='jumbotron3'>
</div>
<table>
<tr>

  <td><h3>Title.</h3></td>
  <td><h3>Text </h3></td>
  <td><h3>Image</h3></td>

  </tr>


<% @items.each do |item| %>
  <tr>

    <td><%= item.title %></td>
    <td><%= truncate(item.text, length: 80, omission: "...")  %></td>
    <td><%= image_tag item.image_url(:thumb).to_s %></td>
    <td><%= link_to 'Show', item ,class: "btn btn-default"%></td>
    <td><%= link_to 'Edit', edit_item_path(item),class: "btn btn-default" %></td>
    <td> <%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' },class: "btn btn-default" %></td>

  </tr>
  <% end %>

</table>

<br>

<%= link_to 'New Item', new_item_path %>
